<Window x:Class="ClientesMixtos.Views.NotaFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientesMixtos.Views"
        mc:Ignorable="d"
        Title="Nueva Nota" Height="320" Width="400" MinHeight="320" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BrushWindowBg}">

    <Border Background="{DynamicResource BrushCardBg}" CornerRadius="12" Effect="{StaticResource CardShadow}"
            BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Margin="16">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Descripción" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,4"/>
            <TextBox Grid.Row="1" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     Height="70" Style="{StaticResource ModernTextBoxStyle}"/>

            <TextBlock Grid.Row="2" Text="Fecha de Creación" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource BrushTextMuted}" Margin="0,12,0,4"/>
            <DatePicker Grid.Row="3" SelectedDate="{Binding FechaCreacion}"
                        Margin="0,0,0,12" Height="34"
                        Style="{StaticResource ModernDatePickerStyle}"/>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Guardar" Width="80" Height="30" Margin="4,0"
                        Command="{Binding GuardarCommand}" 
                        Style="{StaticResource ActionButtonStyle}" IsDefault="True"/>
                <Button Content="Cancelar" Width="80" Height="30" Margin="4,0"
                        Command="{Binding CancelarCommand}" 
                        Style="{StaticResource ActionButtonStyle}" Background="#EF4444" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
